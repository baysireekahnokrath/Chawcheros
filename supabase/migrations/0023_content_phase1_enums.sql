-- ============================================================================
-- 0023 · โมดูลคอนเทนต์ เฟส 1 · ค่า enum ใหม่
-- ============================================================================
-- Bay ตัดขอบเขตเฟส 1 (decisions-log N1-N2) เหลือ ข้อความล้วน · ภาพเดี่ยว · อัลบั้มภาพ
-- และงานผลิตไม่ได้แบ่ง 3 ขั้นแบบวิดีโอแล้ว (requirement Q1 → ส่งต่อ agent)
-- จึงต้องมีขั้นกลางขั้นเดียวว่า "กำลังทำ"
--
-- ค่าวิดีโอเดิมไม่ลบ (กฎ A7) แค่ซ่อนจากหน้าตั้งงาน เปิดกลับได้ตอนทำเฟสวิดีโอ
--
-- ไฟล์นี้ทำอย่างเดียวจริงๆ เพราะ Postgres ใช้ค่า enum ที่เพิ่งเพิ่ม
-- ใน transaction เดียวกันไม่ได้ ต้องให้ commit ก่อน (แบบเดียวกับ 0020)
-- ============================================================================

alter type content.format add value if not exists 'ข้อความล้วน';
alter type content.format add value if not exists 'ภาพเดี่ยว';
alter type content.format add value if not exists 'อัลบั้มภาพ';

alter type content.stage add value if not exists 'กำลังทำ' after 'ไอเดีย';
