-- ============================================================================
-- 0011 · ประกาศ schema marketing ให้ REST API รู้จัก
-- ============================================================================
-- ทำตามที่เตือนไว้ใน 0009 · เพิ่มโมดูลใหม่ทุกครั้งต้องมาเติมที่นี่
-- ลืม = โมดูลสร้างเสร็จแต่เว็บแอปเรียกไม่ได้ และหาสาเหตุยากมาก
-- ============================================================================

alter role authenticator
  set pgrst.db_schemas =
    'public, graphql_public, core, catalog, materials, pricing, cost, marketing';

notify pgrst, 'reload config';
