-- ============================================================================
-- 0020 · เพิ่มสิทธิ์ "อนุมัติคอนเทนต์"
-- ============================================================================
-- Bay ตอบว่าตัวเองเป็นคนอนุมัติคอนเทนต์ แต่อยากเปิดให้เพิ่มคนได้ทีหลัง
--
-- ถ้าใช้สิทธิ์ admin แทน การเพิ่มคนอนุมัติจะกลายเป็นการยกสิทธิ์ทั้งระบบให้เขา
-- รวมถึงเห็นต้นทุนและกำไร ซึ่งไม่ใช่สิ่งที่ตั้งใจ จึงต้องมีสิทธิ์ของตัวเอง
-- วันหน้าเพิ่มคนอนุมัติ = เพิ่ม 1 แถวในตารางสิทธิ์ ไม่ต้องแก้โค้ด
--
-- ไฟล์นี้ทำอย่างเดียวจริงๆ เพราะ Postgres ใช้ค่า enum ที่เพิ่งเพิ่ม
-- ใน transaction เดียวกันไม่ได้ ต้องให้ commit ก่อน
-- ============================================================================

alter type core.capability add value if not exists 'content.approve';
