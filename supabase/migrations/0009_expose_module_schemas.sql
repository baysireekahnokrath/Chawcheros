-- ============================================================================
-- 0009 · ประกาศ schema ของโมดูลให้ REST API รู้จัก
-- ============================================================================
-- ⚠️ ขั้นนี้ห้ามลืม — ย้ายตารางออกจาก public แล้ว REST API ของ Supabase
--    จะมองไม่เห็นตารางเลย เว็บแอปจะเรียกข้อมูลไม่ได้ทั้งระบบ
--
-- ⚠️ เพิ่มโมดูลใหม่เมื่อไหร่ ต้องเติมชื่อ schema ต่อท้ายบรรทัดล่างนี้ด้วย
--    ไม่งั้นโมดูลใหม่จะสร้างเสร็จแต่เรียกใช้ไม่ได้ และจะหาสาเหตุยากมาก
-- ============================================================================

alter role authenticator
  set pgrst.db_schemas = 'public, graphql_public, core, catalog, materials, pricing, cost';

notify pgrst, 'reload config';
