# โมดูลคอนเทนต์ · ด่าน 4 · แผนสร้างของจริง

เขียน 2026-09-23 · **สถานะ: ✅ Bay เลือกลำดับตามแนะนำ 2026-09-23 · R1 → R2 → R3 → R4 → R5 → R6 · ✅ R1 ผ่าน + ขึ้นเว็บจริง (2026-09-23) · ✅ R2 ผ่าน + ขึ้นเว็บจริง (2026-09-23) · ✅ R3 ผ่าน + ขึ้นเว็บจริง (2026-09-23) · ✅ R4 ผ่าน + ขึ้นเว็บจริง (2026-09-23) · ✅ R5 ผ่าน + ขึ้นเว็บจริง (2026-09-23) · 🔨 R6 สร้างแล้ว รอ Bay ลองใช้ (2026-09-23)**
ที่มา: `2-requirements.md` (ผ่าน) · `mockup.html` (ผ่าน 2026-09-23)

---

## ทำไมต้องแบ่งรอบ

ด่าน 4 คือต่อฐานข้อมูลจริง ทดสอบ แล้วขึ้น production ซึ่งเป็นด่านที่แพงที่สุด
ถ้าทำรวดเดียวทั้งหมด พี่จะได้เห็นของจริงครั้งแรกตอนเสร็จหมดแล้ว ซึ่งเป็นปัญหาเดิมที่เราเลิกทำ
**แบ่งเป็น 6 รอบ ทุกรอบใช้งานได้จริงเมื่อจบรอบ** พี่ลองใช้ แล้วค่อยไปรอบถัดไป

---

## 6 รอบ

| รอบ | ได้อะไร | ข้อ requirement | ใช้งานได้เลยไหม |
|---|---|---|---|
| **R1 · ชิ้นงาน** | โครงข้อมูลใหม่ (แบรนด์ · ประเภทภาพ/ข้อความ · ภาพหลายภาพเรียงได้ · ข้อความแยกช่องทาง · pillar · ธีม · นอกแผน) · หน้าตั้งงานแบบเต็ม · หน้าชิ้นงาน · ซ่อนตัวเลือกคลิป | Q-01–04 · Q-10–13 · Q-30–36 · Q-110–112 · Q-116–118 | ✅ ทีมเขียนข้อความเองไปก่อน |
| **R2 · ตรวจ + แชท + โพสต์** | ตรวจทีละส่วน ผ่าน/ไม่ผ่าน · ติ๊กแก้ทีละข้อ · เก็บเวอร์ชัน · แก้หลังผ่าน = ตรวจส่วนนั้นใหม่ · แชทในงาน + @ชื่อ · โพสต์ทีละช่องทาง · ไม่ลงที่นี่แล้ว | Q-40–52 · Q-47–49i · Q-121 | ✅ ครบวงจรแบบคนทำ |
| **R3 · Content agent** | ต่อ Claude · เขียนทุกช่องทางในรอบเดียวตามคู่มือแพลตฟอร์ม · ถามก่อนเขียน · สมุดความคิด (Bay ยืนยัน) · สัมภาษณ์ brand model · @AI ในแชท · ไอเดียด่วน · บันทึกทุกครั้งที่ AI ทำ | Q-05–09b · Q-37–39 · Q-100–109 · Q-113–120 · Q-49f | ✅ agent เริ่มทำงาน |
| **R4 · ปฏิทิน** | 5 มุมมองแบบ Google Calendar · การ์ดรูป + hook + ตัวย่อช่องทาง · กรอง 5 แบบ · สีตาม pillar/แคมเปญ · ลากย้ายวัน | Q-20–29 | ✅ |
| **R5 · หน้าแรก** | กล่อง ⭐ ทั้ง 10 + ข้อความ @ถึงฉัน · หน้าแรกของการตลาดแยกจากของ Bay | Q-123–125 · H1–H6 H9 H10 H16 H19 | ✅ |
| **R6 · แผนเดือน + แบรนด์** | ธีมคร่อมเดือน · แผนเดือนพร้อมเส้นตาย 20 วัน · อนุมัติแผนแล้วสร้างการ์ด · Brand Kit · ตัวอย่างใช่/ไม่ใช่ · ตั้งค่าเส้นทาง agent | Q-14–18 · Q-70–72 · Q-08 | ✅ |

ไม่อยู่ในเฟสนี้: เตรียมถ่ายภาพ ยืมของจากคลัง (Q-60 Q-61) ผมเสนอทำหลัง R6 เพราะต้องแตะโมดูลคลังด้วย

---

## ลำดับที่ผมแนะนำ

```
R1 ชิ้นงาน → R2 ตรวจ+แชท → R3 agent → R4 ปฏิทิน → R5 หน้าแรก → R6 แผน+แบรนด์
```

**เหตุผล**
- R1 กับ R2 คือฐาน ทุกอย่างที่เหลือยืนอยู่บนนี้ และจบ R2 ทีมก็ใช้งานจริงได้ครบวงจรแล้ว
- R3 agent เป็นหัวใจที่พี่อยากได้ ขึ้นเป็นลำดับ 3 เพราะต้องมีที่ให้ agent เขียนลง (R1) และที่ให้พี่ตรวจ (R2) ก่อน
- R5 หน้าแรกดึงข้อมูลจากทุกรอบ ทำหลังๆ จะได้ไม่มีกล่องว่าง

**ทางเลือก:** ถ้าอยากเห็น agent เร็วที่สุด สลับเป็น R1 → R3 → R2 ได้ แต่ช่วงนั้นพี่จะตรวจงานด้วยปุ่มอนุมัติแบบเดิมไปก่อน

---

## สิ่งที่ต้องได้จาก Bay ก่อนเริ่มแต่ละรอบ

| ก่อนรอบ | ต้องการ | ทำไม |
|---|---|---|
| R1 | ไม่มี | — |
| R3 | **Anthropic API key** ใส่เป็น env var ใน Vercel (ผมเขียนขั้นตอนให้) | agent ต้องเรียก Claude · ผมใส่ให้เองไม่ได้ ตามข้อจำกัดใน `docs/deploy-vercel.md` |
| R3 | ตั้งงบค่า AI ต่อเดือน | ✅ Bay ตั้ง 300 บาท/เดือน (2026-09-23) · เพดานอยู่ในระบบ แก้ได้ที่หน้าสมอง agent |
| R3 | เวลา 20-30 นาที ให้ agent สัมภาษณ์ brand model ครั้งแรก | agent จะเขียนได้ดีเมื่อมี brand model |

---

## แต่ละรอบทำงานยังไง

1. ผมเขียน migration + หน้าจอ + ทดสอบ SQL ในเครื่อง
2. push ขึ้น branch แล้วแจ้งพี่ว่าเสร็จอะไร ทดสอบอะไรไปแล้ว อะไรยังไม่ได้ทดสอบ
3. พี่ลองใช้บน production แล้วบอกจุดที่ผิด
4. แก้จนพี่บอกผ่าน แล้วค่อยไปรอบถัดไป

**1 รอบ = 1 session** ตามกฎประหยัด token · เปิด session ใหม่แล้วพิมพ์ว่า *"เริ่มด่าน 4 รอบ R1 โมดูลคอนเทนต์"* ผมอ่านไฟล์นี้เอง

---

## วิธีตอบ

> ลำดับตามที่แนะนำ · API key จะส่งให้ตอนถึง R3

หรือเปลี่ยนลำดับได้ เช่น *"R1 → R3 → R2 → ที่เหลือตามแนะนำ"*

---

## R3 · สร้างแล้ว (2026-09-23)

| ส่วน | ที่อยู่ |
|---|---|
| ฐานข้อมูล | `supabase/migrations/0026_content_agent.sql` · ขึ้น production แล้ว · ทดสอบ `supabase/tests/10_content_agent.sql` (8 ข้อ ผ่าน) |
| ตัวเรียก Claude + งบ | `web/src/modules/content/ai.ts` · เช็กงบกรณีแย่สุดก่อนเรียกทุกครั้ง · บันทึกทุกครั้งลง `content.ai_requests` |
| สมอง + เขียน + ไอเดีย + สัมภาษณ์ | `web/src/modules/content/agent.ts` |
| หน้าจอ | กล่อง Content agent ในหน้าชิ้นงาน · @AI ในแชท · `/content/idea` (ปุ่ม + มุมขวาล่าง) · `/content/brain` |

**รุ่นเริ่มต้น Claude Opus 5** · Bay เปลี่ยนเป็น Sonnet 5 / Haiku 4.5 ได้ที่ `/content/brain` แท็บงบ AI
ค่าต่อครั้งโดยประมาณ (ข้อความ 3 ช่องทางรวมบล็อก): Opus 5 ~5-8 บาท · Sonnet 5 ~2-3 บาท · Haiku 4.5 ~1-1.5 บาท

✅ **Bay ให้ผ่าน 2026-09-23** · ลองจริงแล้ว: สัมภาษณ์ brand model (~0.4 บาท/จังหวะ) · ไอเดียด่วน → agent เขียน FB + บล็อก → ส่งตรวจเอง → Bay อนุมัติ
`ANTHROPIC_API_KEY` อยู่ใน Vercel ทั้ง Production และ Preview · แก้ระหว่างรอบ: หน้าตรวจไม่แสดงข้อความ (ดึงชื่อช่องทางแยก) · กันอนุมัติส่วนที่มองไม่เห็น

---

## R4 · สร้างแล้ว (2026-09-23)

ไม่มี migration · ใช้ข้อมูลเดิม (placements.planned_on)

| ส่วน | ที่อยู่ |
|---|---|
| ปฏิทิน | `web/src/components/content/Calendar.tsx` · 5 มุมมอง · กรอง 5 แบบ · สีตาม pillar/แคมเปญ · แถบสถานะ · ลากย้ายวัน (คอม) · แตะแล้วเลือกวัน (มือถือ) |
| ข้อมูล | `getCalendar()` ใน `queries.ts` · `movePlacements()` ใน `actions.ts` (ช่องทางที่ลงแล้วไม่ย้าย) |
| หน้า | `/content` เปิดเป็นปฏิทิน · `/content?view=board` = กระดานเดิม (Q-28) |

แคมเปญยังไม่มีสีในระบบ · ให้สีจากรหัสแคมเปญแบบคงที่ไปก่อน (R6 Brand Kit ตั้งสีเองได้)

---

## R5 · สร้างแล้ว (2026-09-23)

| ส่วน | ที่อยู่ |
|---|---|
| ฐานข้อมูล | `0027_content_dashboard.sql` · แฟ้มคู่แข่ง (`content.swipes`) · ไอเดียจาก agent (`content.idea_suggestions`) · ขึ้น production แล้ว · ทดสอบ `11_content_dashboard.sql` 3 ข้อผ่าน |
| หน้าแรก | `/content` · `web/src/modules/content/dashboard.ts` · `components/content/HomeBoxes.tsx` |
| เมนู | หน้าแรก · ปฏิทิน (`/content/calendar`) · ตรวจ (`/content/review`) · แบรนด์ (`/content/brain`) · แผนจะเพิ่มใน R6 |

กล่อง: H1 รอตรวจ · H2 agent ถาม (ตอบในกล่อง) · @ถึงฉัน · H3 สมุดรอยืนยัน · H4 แผนเดือน (ที่ว่างรอ R6) · H5 7 วัน (แดง = เสี่ยง) · H6 ลงไม่ครบ · H9 สัดส่วน pillar · H10 สินค้าไม่ได้พูดถึง 60 วัน · H16 แฟ้มคู่แข่ง · H19 ไอเดียจาก agent
การตลาด (ไม่มีสิทธิ์อนุมัติ) เห็น: ตีกลับ · @ถึงฉัน · งานที่ทำอยู่ แทนกล่องของ Bay (Q-125)
แฟ้มคู่แข่ง: agent เปิดลิงก์ Facebook/IG เองไม่ได้ · ทีมวางแคปชันที่เห็น agent สรุปจากตรงนั้น

---

## R6 · สร้างแล้ว (2026-09-23)

| ส่วน | ที่อยู่ |
|---|---|
| ฐานข้อมูล | `0028_content_plan_brand.sql` · แผน (`plans` `plan_slots` `plan_changes`) · หัวข้อรายสัปดาห์ (`theme_weeks`) · Brand Kit (`brand_kits` `brand_examples`) · สีแคมเปญ · เส้นทาง agent (`agent_routes`) · ขึ้น production แล้ว · ทดสอบ `12_content_plan.sql` 5 ข้อผ่าน |
| แผนเดือน | `/content/plan` · `components/content/PlanEditor.tsx` · `modules/content/plan-actions.ts` |
| แบรนด์ | `/content/brain` เพิ่มแท็บ Brand Kit · คลังงาน · เส้นทาง agent · `components/content/BrandKit.tsx` |
| agent | อ่านตัวอย่างใช่/ไม่ใช่ · หัวข้อรายสัปดาห์ของธีม · ข้ามช่องที่ตั้งเป็น "คนทำเอง" |

Q-15: Bay ไม่เอาป้ายส่งช้า → โชว์แค่เส้นตาย (วันแรกของเดือน − 20 วัน)
